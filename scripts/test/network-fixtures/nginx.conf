server {
  listen 80;
  server_name _;
  location / { root /usr/share/nginx/html; add_header Accept-Ranges bytes; }
  location /redirect/same { return 302 /media/sample.mp4; }
  location /redirect/cross { return 302 http://127.0.0.1:18081/media/sample.mp4; }
  location /timeout { return 504; }
}
